[pytest]
testpaths = tests
markers =
    unit: pure logic tests — no external dependencies
    integration: tool-level tests with mocked I/O boundaries
    live: requires a running Open5GS stack (skipped by default)
addopts = -m "not live" -q --tb=short
